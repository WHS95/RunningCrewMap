# 결정 사항 (모두 완료)

---

## ✅ 모든 결정 완료

### 1. Supabase 서비스 키 전환 ✅
- **결정**: 사용자가 직접 `.env`에 `SUPABASE_SERVICE_ROLE_KEY` 추가 완료

### 2. 크루 서비스 데이터 소스 확정 ✅
- **결정**: Supabase로 완전 통합 (FileCrewService JSON 파일 방식 제거)

### 3. 페이지 전환 애니메이션 방식 ✅
- **결정**: 옵션 B — Framer Motion (모든 브라우저 지원)

### 4. PWA 아이콘/스플래시 에셋 ✅
- **결정**: 현재 텍스트 기반으로 생성

### 5. 오프라인 지원 범위 ✅
- **결정**: 옵션 A — 기본 (구현 완료)

### 6. 풀-투-리프레시 구현 ✅
- **결정**: 홈, 크루목록에 우선 적용 (구현 완료)

---

## ✅ 자동 결정 (AI 관리자가 진행)

- ✅ react-icons 완전 제거 → lucide-react 통합
- ✅ BFF 아키텍처: Server Component + Server Actions 패턴 적용
- ✅ 기존 클라이언트 Supabase/서비스 레이어 유지 (admin 페이지용)

---

## 2026-05-18: 새 server-side region 분류 함수의 main_address 우선 순서

- `src/lib/server/regions.ts:regionCodeForCrew`는 `main_address || address` 순서로 우선.
- 기존 `src/lib/utils/region-utils.ts:filterCrewsByRegion`은 `address(=detail_address||main_address)` 우선이라 detail address가 다른 시/도로 시작하면 오분류했음 — 새 함수는 이 버그를 회피하면서 클라이언트 경로(필터)는 기존 함수 그대로 유지.

- 2026-05-22: 매장(러닝 인증 카페·식당) 도메인을 크루와 동일한 자가등록+어드민 승인 패턴으로 도입. 도메인 prefix `store`, 캐시 태그 `STORES_CACHE_TAG` 독립. 카테고리는 cafe/restaurant/pub/other 4종.
